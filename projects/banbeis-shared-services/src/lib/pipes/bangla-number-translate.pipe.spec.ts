import { BanglaNumberTranslatePipe } from './bangla-number-translate.pipe';

describe('BanglaNumberTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new BanglaNumberTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
